# Backend Environment Variables

# MongoDB Connection URI (use MongoDB Atlas for production)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/codeconnect?retryWrites=true&w=majority

# Server Port (set by hosting platform like Render, fallback to 5000)
PORT=5000
